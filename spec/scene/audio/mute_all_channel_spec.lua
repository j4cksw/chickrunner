describe("mute_all_channel", function()
  local mute_all_channel
  
  setup(function()
    mute_all_channel = require("scene.audio.mute_all_channel")
  end)
  
  it("should set volume of all channel to 0")
end)