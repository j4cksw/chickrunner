describe("remove_obstacle", function()
  local remove_obstacle
  
  setup(function()
    remove_obstacle = require("scene.game.obstacle.remove_obstacles")
  end)
  
  it("Evaluate display.remove with every elements in obstacle queue")
end)