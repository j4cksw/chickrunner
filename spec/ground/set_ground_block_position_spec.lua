describe("set_ground_block_position", function ( ... )
	local set_ground_block_position

	setup(function ( ... )
		set_ground_block_position = require("ground.set_ground_block_position")
	end)

	it("Set y position on given block from ground_config")

	it("Set x position of given block by calcualting position of last block in ground_blocks")
end)